# Charts

Generated by `benchmarks/plot_results.py` from a real GPU run and copied here
because `benchmarks/results/` is gitignored (it holds throwaway local runs).
Only the published figures live here, so a chart in the README is always one a
reader can trace back to a recorded run in docs/benchmarks.md.
